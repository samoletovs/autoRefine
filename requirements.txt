# autoRefine dependencies

# Foundry SDK + auth
azure-ai-projects>=1.0.0
# Upper bound: foundry_agent fails closed if runs.create stops declaring
# max_prompt_tokens/truncation_strategy, so a major bump needs re-verification.
azure-ai-agents>=1.1.0,<2.0.0
azure-identity>=1.19.0

# OpenAI-compatible client (used by foundry agent + health scan)
openai>=1.55.0

# Azure cost + monitoring (health scan)
azure-mgmt-costmanagement>=4.0.1

# Config + data
pyyaml>=6.0.2
python-dotenv>=1.0.1
httpx>=0.27.0
tenacity>=9.0.0

# Observability
azure-monitor-opentelemetry>=1.6.0

# Dev tools
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.7.0
# Measures the agent instructions against Azure's 1,024-token prompt-cache
# floor (tests/test_prompt_cache_prefix.py). Tests skip cleanly without it.
tiktoken>=0.7.0
